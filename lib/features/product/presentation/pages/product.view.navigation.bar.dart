import 'package:flutter/material.dart';

class ProductViewNavigationBar extends StatelessWidget {
  const ProductViewNavigationBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
