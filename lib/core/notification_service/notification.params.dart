enum NotificationCategory { message, alert, reminder }

enum NotificationPriority { low, normal, high, urgent }

enum NotificationStatus { unread, read, dismissed, archived }

enum NotificationChannel { push, email, sms, inApp, web }

enum NotificationAction { accept, dismiss, notSet }
